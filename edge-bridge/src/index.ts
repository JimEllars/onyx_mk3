const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }@const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }7const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }7const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }^const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }7const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }7const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }7const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }@const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }9const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }7const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }@const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }\const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }+const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }'const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }zconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }7const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }3const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }*const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }[const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }]const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }8const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }&const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }6const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }$const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }`const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }!const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }5const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }jconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }4const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }2const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }9const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }-const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }|const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }qconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }kconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }?const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }wconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }/const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }1const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }0const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }=const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }gconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }bconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }"const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }:const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }mconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }{const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }yconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }_const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }.const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }uconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }(const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }cconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  })const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  },const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }}const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }fconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }iconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }sconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  } const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }xconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }pconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }oconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }tconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Hconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }aconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }dconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }lconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }rconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }<const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }Econst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }nconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }vconst headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }>const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  };const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }
const headers = new Headers(options.headers || {});
  if (traceId) {
    headers.set("traceparent", traceId);
    headers.set("x-onyx-trace-id", traceId);
  }